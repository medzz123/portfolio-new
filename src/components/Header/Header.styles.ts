import { styled } from '@theme/config';

export const HeaderContainer = styled('header', {
  padding: '0 $20',
  position: 'absolute',
  top: '$60',
  left: '$half',
  width: '$full',
  maxWidth: '$6xl',
  transform: 'translateX(-$sizes$half)',
});
